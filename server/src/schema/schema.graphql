# src/schema.graphql
type Task {
  id: ID!
  created: DateTime!
  updated: DateTime!
  name: String!
  body: String!
  author: String!
  status: TaskStatus!
}

enum TaskStatus {
  pending
  completed
  in_progress
}

scalar DateTime
